#pragma once
// ===========================================================================
//  TEMPLATE — copy to secrets.h (which is gitignored) and fill in.
// ===========================================================================

#define WIFI_SSID "your-network"
#define WIFI_PASS "your-password"

// mDNS hostname -> reach the dashboard at http://<this>.local/
#define MDNS_HOSTNAME "feeder"
